# Automation Insight Documentation

This repository is the generated public documentation mirror for Automation Insight.

Do not edit published files here directly. The private Automation Insight source repository is the documentation source of truth and publishes only the files explicitly approved by its public-site manifest.

- `develop` mirrors approved documentation from the source `develop` branch and is used to verify the generated site before release.
- `main` mirrors approved documentation from the source `main` branch and is the production GitHub Pages source.

The public mirror is intentionally limited to the standalone manual and its required site assets. Synchronization replaces the generated mirror contents, so manual edits in this repository may be overwritten.
